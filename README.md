Google Sheets Vercel API

Setup:

1 npm install
2 Add environment variables in Vercel:
   SHEET_ID
   GOOGLE_CREDENTIALS

API:

GET:
/api/data

POST:
/api/add

Deploy:
Push to GitHub and import into Vercel.
